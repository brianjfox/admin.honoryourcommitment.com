{
  "name": "admin-honoryourcommitment-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Admin backend for Portugal Must Honor Its Commitments — auth, RBAC, people classification, tags, groups, demographics.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node migrations/run.js",
    "seed-admin": "node scripts/seed-admin.js"
  },
  "dependencies": {
    "@fastify/cookie": "^9.3.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/jwt": "^8.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "nodemailer": "^6.9.14",
    "pg": "^8.12.0",
    "pino-pretty": "^11.2.2"
  }
}
